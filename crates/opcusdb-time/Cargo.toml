[package]
name = "opcusdb-time"
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
description = "opcusdb Timeline: ticks, deterministic timers, snapshots, rollback, replay"

[lints]
workspace = true

[dependencies]
