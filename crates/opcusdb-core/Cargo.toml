[package]
name = "opcusdb-core"
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
description = "opcusdb ECS core: generational entities and sparse-set component storage"

[lints]
workspace = true

[dependencies]
